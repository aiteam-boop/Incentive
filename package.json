{
  "name": "incentive-system",
  "version": "1.0.0",
  "description": "Incentive Management System - Separate Website, Shared Database",
  "private": true,
  "scripts": {
    "install:backend": "cd backend && npm install",
    "install:frontend": "cd frontend && npm install",
    "install:all": "npm run install:backend && npm run install:frontend",
    "start:backend": "cd backend && npm start",
    "dev:backend": "cd backend && npm run dev",
    "start:frontend": "cd frontend && npm start",
    "build": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "build:frontend": "cd frontend && npm install && npm run build",
    "seed": "cd backend && npm run seed",
    "dev": "concurrently \"npm run dev:backend\" \"npm run start:frontend\"",
    "dev:all": "concurrently \"npm run dev:backend\" \"npm run start:frontend\"",
    "start": "npm run start:backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}